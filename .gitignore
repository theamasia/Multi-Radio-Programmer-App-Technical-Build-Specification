node_modules/
dist/
release/
cache/
*.log
.DS_Store
.env
.env.local
profiles/*.json
!profiles/.gitkeep
src/main/usb-drivers/installers/*.exe
test/fixtures/*.bin

# Real codeplug dumps are committed deliberately, but never overwrite the
# factory snapshot -- see docs/DUMP-PROCEDURE.md
